{
  "name": "cofrinho",
  "version": "1.0.0",
  "private": true,
  "description": "Cofrinho — controle de gastos (PWA, React via CDN, JSX pré-compilado)",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "@babel/core": "^7.25.0",
    "@babel/preset-react": "^7.24.0"
  }
}
